( defun fun(a b)
   ;let ( (e 1))     (c (* e (sqrt a)) ) ))
	(setf e 1)
    (setf c (* e (sqrt a)))
	(loop 
            
	    ; (setf e c) (+ a 1)
		(+ a 1)
        (   
			when (> a b) 
				(return e)

        )
			

    )
    
)
